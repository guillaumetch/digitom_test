{% extends 'front/base.html.twig' %}
{% block title %}
    Ajout d'une voiture
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col offset-2 col-lg-8 text-center">
            <h1>Ajout d'une voiture</h1>
            <div class="col offset-3 col-lg-6 text-left">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <div class="text-center marg-t-2">
                    <input class="btn btn-success" type="submit" value="Ajouter" />
                    <a class="btn btn-primary" href="{{ path('car_index') }}">Retour à la liste</a>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
