{% extends 'front/base.html.twig' %}
{% block title %}
    Modifier la couleur
{% endblock %}
{% block body %}
    <div class="row">
        <div class="col offset-2 col-lg-8 text-center">
            <h1>Modifier la couleur</h1>
            <div class="col offset-3 col-lg-6 text-left">
                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <div class="text-center marg-t-2">
                    <input class="btn btn-success" type="submit" value="Modifier" />
                    <a class="btn btn-primary" href="{{ path('color_index') }}">Retour à la liste</a>
                </div>
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
{% endblock %}
